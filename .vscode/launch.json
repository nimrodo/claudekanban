{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run ClaudeKanban Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "outFiles": ["${workspaceFolder}/dist-extension/**/*.js"],
      "preLaunchTask": "npm: package:extension"
    }
  ]
}
